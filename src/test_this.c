#include <stdio.h>
#include "hashmap.c"

int main()
{
  printf("size: %li\n", sizeof(header));
  printf("len: %li\n", strlen("This is an example of how long your string key can be. If you don't think this is long enough then I think you might want to rethink your implementation. I still need to type something here :)"));
  return 0;
}